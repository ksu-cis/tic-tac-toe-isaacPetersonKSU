@page
@model TicTacToe.Pages.Checkers.CheckersModel
@{
    ViewData["Title"] = "Checkers";
}

<h2>Checkers game 4 true Apha CHAD gamerz</h2>

<div id="board">
    @for (int y = 0; y < 8; y++)
    {
        @for (int x = 0; x < 8; x++)
        {
            <div class="square @((x + y) % 2 == 0 ? "black": "red")">
                @if(Model.Game.Board[x,y] != null)
                {
                    
                }
            </div>
        }
    }
</div>